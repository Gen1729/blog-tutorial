# メモサイトへようこそ

このメモサイトは、`content/memos` に Markdown ファイルを置くだけで一覧表示できる最小構成です。

- 追加のデータベースは使いません
- サーバー側で Markdown を読み込みます
- Vercel にそのままデプロイしやすい構成です

## できること

今はまだ、サンプルの Markdown をそのまま表示するだけです。必要になったら、後から個別ページや編集画面を足せます。
